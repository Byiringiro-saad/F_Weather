import React from "react";
import styled from "styled-components";

const Background = ({ type, time }) => {
  return <Container></Container>;
};

const Container = styled.div``;

export default Background;
